require "spec_helper"

RSpec.describe Capistrano3::Casica do
  it "has a version number" do
    expect(Capistrano3::Casica::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
